<div class="flex flex-row">
    <div class="hidden md:w-64 md:mb-0 lg:mb-6 lg:flex-shrink-0 lg:flex lg:flex-col lg:mr-4">
        <p class="text-gray-800"><%= readingTime %></p>
        <p class="mt-1 text-gray-900 text-sm"><%= publishDate %></p>
    </div>
    <div class="md:flex-grow">
        <h3 class="text-2xl mb-2"><%= title %></h3>
        <p><%= excerpt.substring(0, 255) %>...</p>
        <%- include('./ReadMoreButton.ejs', { slug, basePath: '/blog' }) %>
    </div>
</div>